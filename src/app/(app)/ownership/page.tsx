import { EmptyScreen } from "@/components/app/empty-screen";

export default function OwnershipPage() {
  return (
    <EmptyScreen
      href="/ownership"
      title="No ownership to review"
      description="Documents your teams create are listed here with who owns them, so a lead can move or edit one without waiting on a permission request."
    />
  );
}
