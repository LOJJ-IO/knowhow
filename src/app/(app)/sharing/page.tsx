import { EmptyScreen } from "@/components/app/empty-screen";

export default function SharingPage() {
  return (
    <EmptyScreen
      href="/sharing"
      title="No sharing rules yet"
      description="Rules decide who is given access to a new document the moment it is created. Build your org chart first and access can follow your real teams."
    />
  );
}
