import { EmptyScreen } from "@/components/app/empty-screen";

export default function OversightPage() {
  return (
    <EmptyScreen
      href="/oversight"
      title="Nothing to report yet"
      description="Document activity across your teams shows up here as it happens, so you stay current without asking anyone for an update."
    />
  );
}
